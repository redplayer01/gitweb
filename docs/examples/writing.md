# 排版与强调

## 强调

**粗体**，*斜体*，`行内代码`，~~删除线~~（若主题支持）。

## 引用

> 这是一段引用。适合摘录原文或提示。

嵌套列表：

1. 外层一
   - 内层 A
   - 内层 B
2. 外层二

## 分隔线

---

## 脚注

若要在正文里用脚注，可在 `mkdocs.yml` 的 `markdown_extensions` 里加入 `footnotes`，再按 Markdown 脚注语法书写。

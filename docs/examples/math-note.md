# 数学公式说明

MkDocs Material 若要渲染 LaTeX，需在 `mkdocs.yml` 里配置 **pymdownx.arithmatex** 等扩展。

当前仓库**未开启**数学公式，下面仅作文本示例，避免构建报错：

- 行内：\( E = mc^2 \)（渲染需扩展）
- 块公式同理需启用对应插件

需要时可查阅 [Material 文档](https://squidfunk.github.io/mkdocs-material/reference/math/) 按需打开。

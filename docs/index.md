# 欢迎

这是首页。在 `docs/` 目录下编辑 **Markdown** 文件，推送到 GitHub 后会自动构建并发布站点。

## 本地预览

```bash
pip install -r requirements.txt
mkdocs serve
```

浏览器打开 <http://127.0.0.1:8000> 即可预览。

## 部署

在仓库 **Settings → Pages** 中，将 **Source** 设为 **GitHub Actions**。推送 `main` 分支后，工作流会构建并部署。
